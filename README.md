# GitHub Pages configuration
# This deploys the sukisoft-web folder as the root of the GitHub Pages site.
# Push this folder content to the gh-pages branch or configure the repo
# source to: Branch = main, Folder = /sukisoft-web

# Optional: custom domain
# Uncomment and replace with your domain if you have one:
# echo "sukisoft.dev" > CNAME
